org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# FEAT-05: tat Kotlin incremental compilation - Kotlin Build Tools API bi loi
# "Could not close incremental caches" / "already registered" khi build song
# song device_info_plus + local_auth_android (bug cua Kotlin Gradle Plugin,
# khong phai loi code). Build se cham hon 1 chut (compile full moi lan) nhung
# on dinh. Xem docs/implementation/FEAT_05_IMPLEMENTATION_PLAN.md.
kotlin.incremental=false
