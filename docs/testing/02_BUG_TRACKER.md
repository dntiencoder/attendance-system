# 02_BUG_TRACKER.md

**Mục tiêu:** Theo dõi các bug thật (defect có kịch bản tái hiện cụ thể), tách biệt khỏi nợ kỹ thuật/cải tiến chung (những thứ đó sống ở `docs/project/01_BACKLOG.md` mục B).

**Phạm vi:** Bug đã biết từ `REVIEW.md` (đã fix hoặc còn mở) + bug mới phát hiện trong quá trình Phase B/C trở đi.

**Khi nào dùng:** Mỗi khi phát hiện hành vi sai khi test (`docs/testing/01_TEST_PLAN.md`) hoặc dùng thực tế — thêm dòng mới ngay, không đợi tổng hợp cuối kỳ.

**Liên kết:** Nguồn gốc — `REVIEW.md` §15 "Potential bugs" + các mục Critical/High khác; đã fix theo — `ROADMAP.md`; kịch bản test liên quan — `docs/testing/01_TEST_PLAN.md`.

---

## Chú giải cột

- **Severity:** Critical / High / Medium / Low.
- **Status:** `Open` / `In Progress` / `Fixed` / `Won't Fix` (có lý do, ví dụ nằm trong phạm vi cố ý loại trừ — xem `docs/decision/01_DECISION_LOG.md`).
- **Regression:** ID test case tương ứng ở `docs/testing/01_TEST_PLAN.md` dùng để xác nhận không tái phát.

---

## Đã Fixed (trước khi tài liệu này được tạo — 2026-07-13)

| Bug ID | Severity | Status | Module | Description | Fix | Regression |
|---|---|---|---|---|---|---|
| BUG-001 | Critical | Fixed | Settings/Rotation | Lưu cấu hình ở Admin Settings xoá mất `rotationStartDate` → reset toàn bộ chu kỳ xoay ca (REVIEW 15.1) | Round-trip `rotationStartDate` khi build `CompanySettingsModel` (commit `0f63721`, P1-02) | MT-11, MT-12 |
| BUG-002 | High | Fixed | Attendance (mobile) | `AttendanceModel.fromFirestore` ép kiểu cứng `Timestamp` → crash toàn bộ danh sách nếu 1 document thiếu field (REVIEW 15.2) | Null-safety + try-catch per-document (commit `7b1e524`, P1-04) | EC-02 |
| BUG-003 | High | Fixed | Settings (admin) | `double.parse`/`int.parse` không try-catch, validator chỉ kiểm tra rỗng (REVIEW 8.1) | Dùng `Validators.numeric` + bọc try-catch (commit `ef37704`, P1-03) | — |
| BUG-004 | Medium-High | Fixed | Router (admin) | Route dev ẩn `/dev/seed-departments` biên dịch vào bản production (REVIEW 7.2/13.5) | Bọc `kDebugMode` (commit `317c317`, P1-06) | MT-23 (tương tự, khác route) |
| BUG-005 | Critical | Fixed | Dev scripts (mobile) | Credential/UID/email thật hardcode trong source (REVIEW 13.1) | Thay bằng `String.fromEnvironment` placeholder (commit `507df9a`, P1-01) | — |
| BUG-006 | High | Fixed | Employee (admin) | Mật khẩu mặc định `123456` gợi ý sẵn cho nhân viên mới (REVIEW 13.4) | Sinh mật khẩu ngẫu nhiên + copy-to-clipboard (commit `f6cb032`, P1-07) | — |
| BUG-007 | High | Fixed | Firestore | Không có Security Rules nào trong repo để xác minh phân quyền server-side (REVIEW 4.2) | Viết + deploy `firestore.rules` (commit `55da6da`, P1-05) | MT-14 đến MT-17 |

## Đang Open

| Bug ID | Severity | Status | Module | Description | Fix đề xuất | Regression (khi fix) |
|---|---|---|---|---|---|---|
| BUG-008 | High | **Fixed** (2026-07-13, commit `2da7827`) | Attendance (mobile) | Race condition lý thuyết: `checkIn()` kiểm tra `existing.exists` rồi mới `.set()`, không dùng transaction — 2 request gần như đồng thời có thể ghi đè nhau thay vì báo lỗi "đã check-in" (REVIEW 15.5) | `runTransaction()` (backlog TD-01) | MT-05 (Pass gián tiếp), TD01-02 (Pass, 2026-07-22, thiết bị thật) |
| BUG-009 | High | **Fixed** (2026-07-13, commit `5d829ae`) | Attendance/Home (mobile) | Hai cơ chế xác định lịch làm việc song song không đồng bộ: `WorkScheduleHelper` hardcode `2026-06-01`, `CompanySettingsModel.getCurrentShift()` đọc `rotationStartDate` từ Firestore (REVIEW 1.4) | Đồng bộ 1 nguồn mốc ngày duy nhất (backlog TD-03) | MT-11, MT-12 (6 mục regression Pass trên thiết bị thật khi đóng TD-03) |
| BUG-010 | Medium | **Fixed** (2026-07-13, commit `7f17397`) | Employee (admin) | `deleteEmployee` không kiểm tra `isActive` trước khi xoá — dễ xoá nhầm hồ sơ đang hoạt động (REVIEW liên quan 4.5) | Thiết kế lại theo Business Data thay vì `isActive` (xem D-012, `docs/decision/01_DECISION_LOG.md`) — backlog TD-02 | TD02-04/05/06 Pass trên bản Web thật |
| BUG-011 | Medium | **Fixed** (2026-07-20, commit `bf64a9e`) | Auth (mobile) | `.toUpperCase()` không rõ lý do trên email trước khi đăng nhập, không khớp cách admin xử lý (REVIEW 15.3) | Xoá `.toUpperCase()`, giữ `.trim()` (backlog TD-05) | Chưa test tay theo yêu cầu (bỏ qua có chủ đích — thay đổi 1 dòng, rủi ro thấp) |
| BUG-012 | Low | **Won't Fix** (2026-07-20) | Leave (admin) | `request.startDate?.day` dùng `?.` thừa trên field không nullable (REVIEW 15.4) | Không sửa — `LeaveRequestModel.startDate` thực sự là `DateTime?`, `?.` đúng và cần thiết. Ghi chú gốc ở REVIEW.md sai tiền đề (xem TD-16, `01_BACKLOG.md`) | — |

| BUG-013 | Medium | **Fixed** (2026-07-13, commit `9a2623e`, cùng task FEAT-04) | Router (mobile) / Demo Time System | Route `/demo-center` trong `app_router.dart:52-56` không bọc `kDebugMode` — chỉ ẩn lối vào UI (tile ở `profile_screen.dart`), route vẫn tồn tại trong router ở bản release. Phát hiện lúc chạy MT-23 (`docs/testing/01_TEST_PLAN.md`) khi thực hiện FEAT-04. Không lộ dữ liệu/không đổi được business logic thật (`ClockService` tự chặn khi `!kDebugMode`), nhưng không đúng thiết kế đã thống nhất và không nhất quán với precedent P1-06 (`attendance_admin` bọc cả route registration, không chỉ UI entry) | Bọc `if (kDebugMode) GoRoute(path: '/demo-center', ...)` giống hệt pattern P1-06 — đã sửa ngay trong cùng task, không phải việc tồn đọng | MT-23 Pass sau khi sửa (cài/chạy thử release APK thật, không truy cập được route) |

| BUG-014 | Medium | **Fixed** (2026-07-22, xác nhận qua test tay TD01-02) | GPS (mobile) / `checkIn()` | `GpsService.getCurrentPosition()` (`gps_service.dart:27-32`, `timeLimit: Duration(seconds: 15)`) ném `TimeoutException` thô, không được bọc thành thông báo tiếng Việt thân thiện. Ban đầu nghi do double-tap gây tranh chấp 2 yêu cầu GPS đồng thời (TD01-02), nhưng cùng lỗi cũng tái hiện ở **1 lần gọi đơn lẻ, không đồng thời** khi test offline (TD01-04/TD01-08) — cho thấy nguyên nhân có khả năng cao hơn là **GPS không lấy được vị trí trong 15s ở điều kiện tín hiệu/mạng hiện tại**, không hẳn do concurrency | Đã bọc `TimeoutException` bằng thông báo thân thiện + `AppLogger` trong `GpsService.getCurrentPosition()` (commit `5e96441`). **Câu hỏi gốc đã đóng (2026-07-22):** TD01-02 chạy lại trên thiết bị thật, tín hiệu GPS tốt → double-tap Check In chỉ 1 lần thành công, không timeout, không ghi đè — xác nhận nguyên nhân trước đây đúng là tín hiệu yếu, `runTransaction()` (TD-01) xử lý đồng thời đúng như thiết kế. TD01-04/TD01-08 (kịch bản offline riêng, không phải concurrency) vẫn để ngỏ, không phải trọng tâm của BUG-014 | TD01-02 Pass (2026-07-22). TD01-03/04/08 vẫn cần thiết bị thứ 2/điều kiện offline riêng để test — xem `01_TEST_PLAN.md` |

| BUG-015 | High | **Fixed** (2026-07-20, commit `b742193`, cùng task FEAT-01) | Leave (mobile) | `LeaveRepository.getMyLeaveRequests()` dùng `.where('uid', isEqualTo: uid).orderBy('createdAt', descending: true)` — tổ hợp filter+sort này cần 1 composite index mà `firestore.indexes.json` chưa khai báo cho `leave_requests` (chỉ có cho `attendance`), khiến stream lỗi `failed-precondition` ngay từ đầu. Phát hiện qua test tay: admin duyệt/từ chối đơn nhưng mobile không thấy trạng thái cập nhật lại. Phát hiện và sửa ngay trong cùng task, không phải việc tồn đọng | Bỏ `.orderBy()` khỏi query Firestore, sort lại bằng code sau khi tải — giống pattern đã dùng ở `attendance_history_provider.dart` (thay vì thêm composite index mới, tránh phải deploy Firestore) | Test tay end-to-end (tạo đơn mobile → duyệt admin → cập nhật mobile) Pass sau khi sửa |

| BUG-016 | High | **Fixed** (2026-07-22, xác nhận qua test tay) | Home/Attendance (mobile) | `CheckinCard`'s nút Check Out chỉ bật khi `todayAttendance != null`, nhưng `getTodayAttendance()` chỉ tìm document của Business Date **hiện tại** — không hề biết tới document ca đêm hôm qua còn dở dang. Ngay khi Business Date rollover (ví dụ 08:05, vừa qua giờ tan ca đêm 08:00), `todayAttendance` về `null` → nút Check Out biến mất hoàn toàn, dù `checkOut()` đã có sẵn logic ân hạn (`_checkOutGracePeriod`, sau đó giảm còn 1 giờ) cho phép Check Out ca đêm hôm qua muộn — logic đó thành "chết", không ai bấm tới được qua UI bình thường. Phát hiện qua chính user hỏi lại khi test MT-06 bằng Demo Time (nhảy sang 8h05 sáng hôm sau) | Trích xuất logic tìm document (candidate hôm nay, else hôm qua nếu ca đêm còn trong khung ân hạn) thành `_findRelevantAttendanceDoc()` dùng chung bởi cả `getTodayAttendance()` và `checkOut()` — UI giờ nhìn thấy đúng trạng thái "còn ca đêm cần Check Out" thay vì `null` | Test tay Pass (2026-07-22, kịch bản Check In tối Thứ 2 → Check Out sáng Thứ 3 trước giờ hết ân hạn) — xem thêm BUG-017/018/019 (các lớp chặn liên quan phát hiện tiếp ngay trong cùng chuỗi test) |

| BUG-017 | High | **Fixed** (2026-07-22, xác nhận qua test tay) | Home (mobile) | `isOffDay` (`home_screen.dart`) tính theo **ngày lịch hôm nay**, không biết gì về ca đêm hôm qua còn dở dang. `CheckinCard`'s nút Check Out có điều kiện `canTap: !isOffDay && hasCheckedIn && !hasCheckedOut` — hễ hôm nay là ngày nghỉ bắt buộc, nút Check Out bị khoá **vô điều kiện**, kể cả khi có ca đêm hôm qua hợp lệ (ngày làm việc thật) cần đóng — đúng tình huống vừa được BUG-016 mở khoá lại (`hasCheckedIn` giờ đúng là `true`) nhưng bị `isOffDay` chặn tiếp ở lớp sau. Phát hiện qua user hỏi trực tiếp kịch bản "làm ca đêm trước ngày nghỉ" | Bỏ điều kiện `isOffDay` khỏi phần Check Out trong `checkin_card.dart` (giữ nguyên cho Check In — không được bắt đầu ca mới vào ngày nghỉ, nhưng hoàn tất ca đã bắt đầu hợp lệ thì luôn được phép). `isDisabled` đổi thành `isOffDay && !hasCheckedIn` để không xám nhầm khi có ca cần đóng | Test tay Pass (2026-07-22): ca đêm ngay trước ngày nghỉ bắt buộc → sáng hôm sau (ngày nghỉ) nút Check Out hiện, bấm được, đóng ca đúng |

| BUG-018 | High | **Fixed** (2026-07-22, xác nhận qua test tay) | Attendance (mobile) | Regression tự gây ra khi sửa BUG-016: `_findRelevantAttendanceDoc()` loại trừ document hôm qua nếu `checkOut != null` — đúng cho lúc tìm mục tiêu ĐỂ GHI Check Out, nhưng vì hàm này còn dùng chung cho `getTodayAttendance()` (hiển thị UI), nên ngay sau khi Check Out ca đêm carryover thành công, lần refresh UI kế tiếp cũng bị loại trừ luôn document vừa ghi → `todayAttendance` về `null` → Home không cập nhật giờ Check Out vừa xong, coi như "chưa check out". Phát hiện qua user test tay đúng kịch bản Check In tối Thứ 2 → Check Out sáng Thứ 3 trước 9h | Bỏ điều kiện `checkOut != null` khỏi `_findRelevantAttendanceDoc()`, chỉ giữ `shift != 'night'` — việc "đã Check Out rồi không cho ghi lại" đã được `checkOut()` tự kiểm tra riêng nên không mất bảo vệ | Test tay Pass (2026-07-22): Home cập nhật đúng giờ Check Out + "Đã làm Xh giờ" ngay sau khi ghi thành công |

| BUG-019 | Medium | **Fixed** (2026-07-22, xác nhận qua test tay) | Home (mobile) | Cùng nguyên nhân với BUG-017 nhưng ở box Check In: `subText` ưu tiên kiểm tra `isOffDay` trước `hasCheckedIn`, nên khi ca đêm hôm qua carryover sang ngày nghỉ bắt buộc hôm nay, dù `todayAttendance`/`hasCheckedIn` đã đúng, box Check In vẫn hiện "Nghỉ bắt buộc" thay vì thông tin Check In thật ("Cách CT: Xm") — giờ Check In tuy hiện đúng nhưng dòng phụ gây hiểu lầm. Phát hiện qua user test tay ngay sau khi xác nhận BUG-018 | Đổi thứ tự ưu tiên trong `checkin_card.dart`: `hasCheckedIn` được kiểm tra trước `isOffDay`/`isShiftEnded` ở cả `subText` và `isDisabled` của box Check In | Test tay Pass (2026-07-22): ca đêm carryover sang ngày nghỉ bắt buộc → box Check In hiện đúng giờ + "Cách CT: Xm" |

| BUG-020 | Medium | **Fixed** (2026-07-22, sửa 2 lần) | Auth/Home/Attendance (mobile) | `homeProvider`/`attendanceProvider`/`attendanceHistoryProvider` không phải `.autoDispose`, và `AuthNotifier` trước đây không invalidate gì khi đổi tài khoản — nên đăng nhập tài khoản khác vẫn tạm hiện dữ liệu (tên, phòng ban, thống kê, ca, Check In/Out...) của tài khoản CŨ cho tới khi có gì đó chủ động trigger reload. Phát hiện qua user test tay MT-11 (đổi tài khoản nhóm A ↔ B). **Lần sửa đầu (invalidate trong `signOut()`) gây regression mới:** nếu Home vẫn đang "watch" đúng lúc `signOut()` chạy, provider dựng lại NGAY khi `currentUser` đang `null` (giữa lúc đăng xuất/đăng nhập lại) → `_loadUser()` thấy chưa đăng nhập, bỏ qua, không tự thử lại → kẹt rỗng, phải vuốt refresh thủ công | Chuyển việc `ref.invalidate()` từ `signOut()` sang **ngay sau khi `signIn()` thành công** — đảm bảo `FirebaseAuth.instance.currentUser` chắc chắn đã là tài khoản mới trước khi 3 provider dựng lại, tránh hẳn khoảng trống `null` | Test tay Pass (2026-07-22): đăng xuất A → đăng nhập B → Home/Hồ sơ hiện đúng ngay toàn bộ dữ liệu của B, không cần vuốt refresh |

| BUG-021 | Medium | **Fixed** (2026-07-22, xác nhận qua test tay) | Attendance (mobile) | `getTodayAttendance()` không có `try/catch` dịch `FirebaseException` như `checkIn()`/`checkOut()` đã có — khi offline, Home tự gọi hàm này lúc mount (qua `loadTodayAttendance()`) và lộ thẳng `[cloud_firestore/unavailable] The service is currently unavailable...` ra `attendanceState.error`, đúng chỗ SnackBar hiển thị lỗi Check In/Out. Vì lệnh gọi này chạy TRƯỚC khi user bấm Check In, user thấy thông báo thô hiện trước, rồi thông báo thân thiện từ chính lần bấm Check In hiện sau (đã dịch đúng nhờ fix trước đó) — gây tưởng nhầm là 2 lỗi khác nhau. Phát hiện qua user test tay EC-03 (Airplane Mode) | Bọc `getTodayAttendance()` bằng try/catch, dịch `unavailable` sang thông báo tiếng Việt giống `checkIn()`/`checkOut()` | Test tay Pass (2026-07-22): Airplane Mode → mở Home → chỉ còn đúng 1 thông báo tiếng Việt thân thiện |

## Mẫu thêm bug mới

```
| BUG-0XX | <Critical/High/Medium/Low> | Open | <module> | <mô tả + cách tái hiện> | <chưa có / đề xuất> | <ID test liên quan hoặc "chưa có"> |
```

Bug mới phát hiện trong Sprint 4-5 (`docs/project/02_SPRINT.md`, chạy `docs/testing/01_TEST_PLAN.md`) hoặc trong Demo Time System (12 file chưa từng test tay — khả năng cao phát sinh bug mới ở đây) sẽ được thêm vào bảng "Đang Open" theo đúng mẫu trên, đánh số tiếp từ `BUG-013`.
